<% content_for :title do %>
    <%= @page_title = "Все фильмы онлайн в высоком качестве здесь" %>
<% end %>

<% content_for :body do %>
 <!--  <div ng-controller="filmsctrl">
    <ul class="thumbnails">
      <% @films.each do |film| %>
          <li class="span2">
            <div ng-mouseover="showdescription()" class="thumbnail" style="height:285px;">
              <%= link_to (image_fu film.photo,'300x200', :'data-src'=>'holder.js/260x180'), refinery.films_film_path(film), :class=>"thumbnail" %>
              <h5><%= film.name %></h5>
            </div>
          </li>

      <% end %>
    </ul>
     </div>
    <div class="clearfix"></div>
    <%= will_paginate @films %>
    -->
    <div ng-controller="FilmsCtrl">
      <ul class="thumbnails">
        <li ng-repeat="film in films" class="span2">
          <div ng-mouseover='showDescription(film.id)' class="thumbnail" style="height:285px;">
            <h5>{{film.name}}</h5>
          </div>
        </li>
      </ul>

      <div dialog>
        <h1>Описание: </h1>
        <p>{{desc.description}}</p>
      </div>

    </div>
<% end %>


<%= render '/refinery/content_page' %>
